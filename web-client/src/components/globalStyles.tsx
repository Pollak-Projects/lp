"use client";

import "@/src/styles/globals.css";
const GlobalStyles = () => {
  return <></>;
};

export default GlobalStyles;
//makes the css work
//I mean tailwind
//yes, the tailwind doesn't work without this
//idk why, don't ask