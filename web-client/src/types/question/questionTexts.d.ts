import { QuestionCheckboxesDTO } from "@/src/types/question/questionCheckboxes";

export interface QuestionTextsDTO {
  id: string,
  quiz: string,
  title: string,
  answer: string,
  createdBy: string,
  createdAt: string
}
